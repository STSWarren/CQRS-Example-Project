CREATE TABLE placements (id text NOT NULL PRIMARY KEY, show_id text NOT NULL FOREIGN KEY REFERENCES shows(id), dog_id text NOT NULL FOREIGN KEY REFERENCES dogs(id), category text NOT NULL, place int NOT NULL, UNIQUE (show_id, dog_id, category));
